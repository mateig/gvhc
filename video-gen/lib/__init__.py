"""Helpers for video/image generation.

The Gemini client reads its API key from the ``GEMINI_API_KEY`` environment
variable. Export it before running any of the entry points, e.g.::

    export GEMINI_API_KEY=...
"""
